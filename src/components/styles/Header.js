import styled from 'styled-components';

const Input = styled.header`
  align-items: center;
  color: #FFF;
  display: flex;
  padding: 16px 0 40px;

  img {
    margin-right: 10px;
  }
`;

export default Input;
